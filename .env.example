# No .env is needed for local D1. Copy .dev.vars.example to .dev.vars instead.
# Cloudflare CLI authentication uses `wrangler login`; runtime DB access uses bindings.
# Never put API_TOKEN in a NEXT_PUBLIC_ variable.
